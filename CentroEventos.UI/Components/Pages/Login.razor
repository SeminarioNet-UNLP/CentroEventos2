
@page "/Login"
@rendermode InteractiveServer


<Modal @ref="modal" Title="Iniciar sesion" IsVerticallyCentered="true" UseStaticBackdrop="true">
    <BodyTemplate>

       @if(!isRegistro)  
       {
        <div class="row g-3 align-items-center body-custom">
            <div class="row-auto">
                <label for="inputUsuario" class="col-form-label">Usuario</label>
            </div>
            <div class="row-auto">
                <input class="form-control" type="text" placeholder="Default input" aria-label="default input example">
            </div>
            <div class="row-auto">
                <label for="inputPassword6" class="col-form-label">Password</label>
            </div>
            <div class="row-auto">
                <input type="password" id="inputPassword6" class="form-control" aria-describedby="passwordHelpInline">
            </div>
        </div>
       }
        @if(isRegistro)
        {
            <RegistrarUsuario/>
        }
    </BodyTemplate>

    <FooterTemplate>
        @if(!isRegistro){
        <Button Color="ButtonColor.Secondary" @onclick="OnHideModalClick">Close</Button>
        <Button Color="ButtonColor.Secondary" @onclick="IsRegistro">Registrar</Button>
        <Button Color="ButtonColor.Primary">Save changes</Button>
        }
    </FooterTemplate>
    
</Modal>

<Button Color="ButtonColor.Primary" @onclick="OnShowModalClick">Open Login Modal</Button>

@code {
    bool isRegistro = false;
    private Modal? modal;
   
    private void  IsRegistro()
    {
        isRegistro = true;
        StateHasChanged();
    }
    private async Task OnShowModalClick()
    {
        await modal?.ShowAsync();
    }

    private async Task OnHideModalClick()
    {
        isRegistro = false;
        await modal?.HideAsync();
        
    }
}
